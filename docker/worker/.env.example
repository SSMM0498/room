# Environment mode (DEV or PROD)
ENV=DEV

# Workspace directory
# In DEV mode, this should point to your local workspace folder
# In PROD mode, this will be set by the container
WORKER_WORKSPACE_DIR=/workspace

# Server port
PORT=3002
